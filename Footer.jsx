import { sectionWidth } from "../styles"

const Footer = () => {
    return (
        <footer className= {`${sectionWidth}`}>
            <div className="text-white text-center">App by Charles Acosta</div>
        </footer>
    )
}

export default Footer